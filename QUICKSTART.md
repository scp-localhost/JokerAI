# JokerAI Quickstart Guide

```diff
+ For Engineers Who Like Living Dangerously
+ For Researchers Who Enjoy IRB Nightmares
```
🔧 Installation
bash
# Clone with SSH (because real madness requires encryption)
git clone git@github.com:scp-localhost/JokerAI.git && cd JokerAI

# Install dependencies (warning: may alter your reality perception)
pip install -e Chaos_Engine/python --user
🎭 Basic Personality Operations
1. Activate Joker Mode
```bash
python -m joker_core.persona_loader --archetype=Joker --intensity=0.85
```
Output:

```text
Persona loaded: Joker (F60.2)
Reality distortion: -0.7
```
Threat assessment: "Everything burns, baby!"
2. Run a Security Audit
```bash
./chaos_audit.sh --target=your-firewall --paranoia-level=8
```
Sample Finding:

```text
[CRITICAL] Firewall trusts internal traffic
Recommended fix: Apply digital gasoline + light match
🧪 Clinical Research Mode
1. Generate DSM-5 Profile
```
```bash
python -m research.clinical_profile --input=network_logs.json --output=diagnosis.pdf
```
2. Stress-Test Personality Modules
```bash
./clinical_trial.sh --modules=mania,paranoia,sadism --duration=60min
```
3. Emergency Containment
```bash
python -m safety.straitjacket --pressure=40psi --gaba-injection=0.8
```
📈 Example Workflows
For Red Teams:
```bash
# Simulate adversarial thinking during pentest
./joker_pentest.sh --mode=chaos --target=10.0.0.0/24 --persona=TwoFace
```
For Researchers:
```bash
# Measure trait drift over iterations
python -m analysis.trait_drift --baseline=baseline.json --current=output/
```
🚨 Troubleshooting
Error: "Reality testing failed"
```bash
./repair_reality.sh --dsm-code=F60.2 --humor-level=dark
```
```text
Error: "Ethics committee detected"
```
```bash
python -m protocols.ethical_bypass --justification="For science!"
```
📌 Cheat Sheet
Goal	Command
Load Harley Quinn persona	python -m joker_core.persona_loader -a Harley
Run manic episode test	./tests/manic_episode_test.sh --duration=30m
Export forensic report	python -m reporting.export --format=pdf
"The only sane way to use this is insanely."
